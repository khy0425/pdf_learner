rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 테스트 모드: 모든 인증된 사용자에게 모든 문서에 대한 접근 허용
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}